# Template for frontend/.env — copy to .env and fill in.
#   cp .env.example .env
#
# These only pre-fill the login form during development. Leave them blank
# and the form simply starts empty.
VITE_DEMO_EMAIL=
VITE_DEMO_PASSWORD=
